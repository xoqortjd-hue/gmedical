# PRD - 금양메디칼 의료기기 재고관리 시스템

## 1. 제품 개요

**제품명**: 금양메디칼 의료기기 재고관리 시스템
**도메인**: https://keumyang.cloud
**사용자**: 금양메디칼 영업팀, 관리자, 대표
**목적**: 의료기기(수술용 장비)의 입출고, 재고 현황, 병원별 배치 상태를 실시간으로 관리

## 2. 핵심 기능

### 2.1 장비 입출고 관리
- **출고 처리**: 장비 선택 → 병원 선택 → 담당자 선택 → 사진 촬영 → 출고 완료
- **입고 처리**: 병원에서 사무실로 장비 반납 처리
- **다중 장비 일괄 출고**: 여러 장비를 한 번에 출고 처리
- **담당자 관리**: DB 기반 담당자 목록 (신규 입력 시 자동 등록)
  - 등록 담당자: 박성현, 이호진, 하준혁, 정성엽, 이주호, 이정국, 오형준, 조용원, 하수천

### 2.2 리포트 (장비 관리 보고서)
- **병원별 이행 완료건수**: 장비가 병원 → 사무실로 입고된 횟수 = 수술 1건
- **장비 입출고 현황**: 전체 장비의 현재 위치, 이동 경로, 담당자
- **특이사항**: 주간/월간 보고 시 관리자가 작성하는 텍스트 메모 (DB 저장, 기간별 관리)
- **영업팀 장비 입출고 현황판**: 장비 그룹별 입고/출고 상태 시각화
- **섹션별 인쇄 선택**: 각 섹션 포함/제외 체크박스로 필요한 부분만 인쇄
- **기간 탐색**: 이전/다음 버튼으로 과거 주차 리포트 조회 가능
- **제외 필터**: 병원/장비별 개별 제외 (인쇄 시 미포함)

### 2.3 CEO 대시보드
- 병원별 장비 배치 현황 요약
- 전체 장비 입고/출고 통계

### 2.4 영업팀 현황판
- 장비 그룹별 (ZENIUS MIS, ILIAD 등) 입고/출고 상태 그리드
- 색상 코드: 녹색=입고, 빨강=출고

### 2.5 제품/장비 등록 관리
- 바코드(GS1) 스캔 기반 장비 등록
- 카테고리: EQUIPMENT(장비), CONSUMABLE(소모품), BIOLOGIC(생물학적)

### 2.6 사진 관리
- 출고/입고 시 사진 촬영 필수 (증빙)
- 연속 촬영 모드, 앨범 다중 선택
- 확대 보기 (스와이프 갤러리)

## 3. 사용자 흐름

### 3.1 영업팀 (모바일)
```
모바일 접속 → 출고 처리 선택 → 장비 선택 → 병원/담당자 선택 → 사진 촬영 → 출고 완료
                             → 입고 처리 선택 → 장비 선택 → 입고 완료
```

### 3.2 관리자 (데스크톱)
```
리포트 페이지 → 주간/월간 선택 → 특이사항 작성/저장 → 인쇄할 섹션 선택 → 인쇄하기
```

### 3.3 대표 (데스크톱/모바일)
```
CEO 대시보드 → 전체 현황 확인
인쇄된 보고서 수령 (업체별 완료건수 + 입출고 현황 + 특이사항 + 영업팀 현황판)
```

## 4. 기술 아키텍처

```
[사용자 브라우저] → [Nginx (443/SSL)] → [Express.js (5000)] → [SQLite DB]
                         ↑
              keumyang.cloud (가비아)
              Elastic IP: 3.35.128.229
              Let's Encrypt SSL 인증서
```

## 5. 데이터 모델

### 핵심 테이블
| 테이블 | 용도 |
|--------|------|
| `products` | 장비/제품 정보 (바코드, 이름, 카테고리) |
| `hospitals` | 병원 목록 (id=2: 부산사무실) |
| `lending_items` | 장비별 현재 대여 상태 |
| `lending_movements` | 장비 이동 이력 (출고/입고/이동) |
| `staff_members` | 담당자 목록 (이름, 활성 상태) |
| `report_notes` | 리포트 특이사항 (기간별 저장) |
| `lending_item_photos` | 장비 사진 |

### 비즈니스 규칙
- **수술 1건** = 장비가 병원 → 사무실(id=2)로 입고된 건
- **입고 상태** = `hospital_id === 2`
- **장비 중복 제거** = 같은 `product_name`이면 최신 `deploy_date` 것만 유지
- **장비 정렬 우선순위** = ZENIUS MIS > ZENIUS CEMENT SCREW > ILIAD > OLIF 순

## 6. 배포 환경

| 항목 | 값 |
|------|-----|
| 서버 | AWS EC2 (Ubuntu) |
| IP | 3.35.128.229 (Elastic IP) |
| 도메인 | keumyang.cloud (가비아) |
| SSL | Let's Encrypt (자동 갱신) |
| 웹서버 | Nginx → Express 프록시 |
| 프로세스 | PM2 (inventory-system, cloudflare-tunnel) |
| Git 브랜치 | clean-main |

## 7. 최근 변경 이력 (2026-03)

| 날짜 | 변경 내용 |
|------|-----------|
| 2026-03-22 | 담당자 관리 DB화 (staff_members 테이블) |
| 2026-03-22 | 리포트 기간 이동 (이전/다음) 버튼 추가 |
| 2026-03-22 | 리포트 특이사항 DB 저장 (주차별 관리) |
| 2026-03-22 | 리포트 섹션별 인쇄 선택 체크박스 추가 |
| 2026-03-22 | 리포트 특이사항 입력/인쇄 기능 추가 |
| 2026-02-11 | 모바일 리포트 병원/장비 체크박스 필터 |
| 2026-02-10 | 리포트 장비 제외 필터 + 출고 시 기존 사진 삭제 |
| 2026-02-09 | 다중 장비 일괄 출고 기능 |
